jQuery(document).ready(function(){

  $("input#secretCode").on("change", function(e){
    if(e.target.value.length >= 3){
      console.log("Made it");
      $.ajax({
        url: "/test"
      });
    }
  })

})
